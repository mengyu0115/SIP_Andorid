<?xml version="1.0" encoding="utf-8"?>
<paths>
    <!-- 外部存储下载目录 -->
    <external-path
        name="external_download"
        path="Download/SIP_Messenger" />

    <!-- 外部存储根目录 -->
    <external-path
        name="external"
        path="." />

    <!-- 缓存目录 -->
    <cache-path
        name="cache"
        path="." />

    <!-- 外部缓存目录 -->
    <external-cache-path
        name="external_cache"
        path="." />
</paths>
